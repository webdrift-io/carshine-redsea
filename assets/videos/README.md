# Hero Video Assets

Place the production hero background video here as:

- `carshine-hero.mp4`
- optional optimized fallback: `carshine-hero.webm`

The landing pages already include a muted, looping hero video layer.
After the final file is added, wire the source tags or enable runtime loading during the final media pass.
